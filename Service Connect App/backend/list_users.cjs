const mongoose = require('mongoose'); const bcrypt = require('bcryptjs'); mongoose.connect('mongodb://127.0.0.1:27017/service-connect').then(async () => { const salt = await bcrypt.genSalt(10); const hash = await bcrypt.hash('password123', salt); const User = mongoose.connection.collection('users'); await User.updateOne({email: 'testcustomer@example.com'}, {$set: {password: hash}}); console.log('Updated'); process.exit(0); });
